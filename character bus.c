#include<stdio.h>
int main()
{
    int n;
    scanf("%d", &n);
    int a[n+1],b[2n];
    char c[2n+1];
    for(i=1; i<=n; i++)
        scanf("%d", &a[i]);
    scanf("%s", c);
    for(i=0; i<2n; i++)
    {
        if(c[i]=='0')
        {

        }
    }
}
